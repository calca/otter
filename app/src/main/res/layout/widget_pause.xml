<?xml version="1.0" encoding="utf-8"?>
<!--
    I widget usano RemoteViews: supportano solo un sottoinsieme di View
    (FrameLayout, LinearLayout, RelativeLayout, TextView, ImageView, Button,
    ProgressBar…). NO MaterialButton, NO ConstraintLayout, NO ViewBinding.
    I colori hardcoded sono intenzionali: i widget non supportano ?attr/.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Sfondo arrotondato -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/widget_background"
        android:padding="6dp">

        <!-- STATO IDLE: nessuna sessione attiva -->
        <LinearLayout
            android:id="@+id/widgetIdle"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="visible">

            <!-- Decorativa: il testo qui sotto porta già la stessa informazione,
                 stesso trattamento delle icone equivalenti nel widget Glance
                 vero (PauseWidgetProvider.kt, contentDescription = null). -->
            <ImageView
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:src="@drawable/ic_otter_widget"
                android:contentDescription="@null" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_label_idle"
                android:textSize="11sp"
                android:textColor="#2C4A3E"
                android:textStyle="bold"
                android:layout_marginTop="2dp" />
        </LinearLayout>

        <!-- STATO ACTIVE: sessione in corso -->
        <LinearLayout
            android:id="@+id/widgetActive"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <!-- Decorativa, stesso motivo dell'otter idle qui sopra. -->
            <ImageView
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:src="@drawable/ic_pause_widget"
                android:contentDescription="@null" />

            <TextView
                android:id="@+id/widgetTimeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="#3D7A5C"
                android:textStyle="bold"
                android:layout_marginTop="2dp" />
        </LinearLayout>

    </LinearLayout>
</FrameLayout>
